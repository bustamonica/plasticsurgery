pillow>=10.0
# <5 is a compatibility pin, not a feature pin: nothing here needs the Haar
# CascadeClassifier any more (the face stage was removed 2026-08-14), but the
# corpus tooling has only ever been measured against OpenCV 4. censorship.py's
# thresholds in particular are calibrated against its decoder, and AGENTS.md
# requires a corpus-wide re-measurement before those move.
opencv-python-headless>=4.9,<5
numpy>=1.26

# Gallery scraping (training/scripts/scrape_gallery.py):
requests>=2.31
beautifulsoup4>=4.12

# Dev/test only (not needed to run the pipeline itself):
pytest>=8.0
pyyaml>=6.0
# Checks meta.json against dataset_schema.json in the test suite. The pipeline
# itself validates in ingest.py, which stays dependency-free on this.
jsonschema>=4.0
